import { ChartConfig } from "@/components/ui/chart";

export const chartConfig: ChartConfig = {
    quantity: {
        label: "Quantity",
        color: "var(--chart-1)",
    },
    prediction: {
        label: "Prediction",
        color: "var(--chart-2)",
    },
};